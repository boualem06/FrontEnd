import React from "react";
import {withRouter} from "react-router-dom"
const Test = () => {
    return ( <div>
        if you see this that means you are connected 
    </div> );
}
 
export default withRouter(Test) ;